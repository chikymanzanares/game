{
  "name": "stranger-escape",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build",
    "lint": "echo 'No linting needed for monolithic HTML' && exit 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
